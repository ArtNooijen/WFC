{
  "tasks": {
    "dev": "deno run --watch --allow-net --allow-read --allow-env=PORT server.ts",
    "start": "deno run --allow-net --allow-read --allow-env=PORT server.ts",
    "check": "deno check server.ts public/app.js && deno test tests/"
  },
  "fmt": {
    "lineWidth": 100,
    "semiColons": true,
    "singleQuote": false
  }
}
